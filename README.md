# Building-a-User-Authentication-Flow-with-JS

# User Checker & Dashboard App 🔐

A lightweight, multi-page web application that simulates a user registration, login, and dashboard flow using **Vanilla JavaScript**, **HTML5**, and **CSS3**.

## 🌟 Features

- **User Registration:** Collects user details (Name, Email, Password, Age, City).
- **Client-Side Navigation:** Passes user data between pages using URL Query Parameters.
- **Dynamic Login:** Validates user input against the "registered" data.
- **Personalized Dashboard:** Displays user-specific information upon successful login.
- **Responsive Design:** Includes a modern, animated login interface with CSS Keyframe animations.

## 🛠️ Tech Stack

- **HTML5:** Semantic structure for forms and dashboards.
- **CSS3:** Custom styling featuring a unique morphing background animation on the login page.
- **JavaScript (ES6+):** - DOM Manipulation
  - Event Listeners
  - `URLSearchParams` for data handling
  - Template Literals for dynamic UI updates

## 📂 Project Structure

- `register.html`: The starting point for new users.
- `login.html`: The gateway where users verify their credentials.
- `dashboard.html`: The final landing page displaying user data.
- `register.js`: The central logic handling the flow for all three pages.
- `style.css`: Modern styling and animations.

## 🚀 How to Run

1. Clone the repository:
   ```bash
   git clone [https://github.com/your-username/user-checker-app.git](https://github.com/your-username/user-checker-app.git)
